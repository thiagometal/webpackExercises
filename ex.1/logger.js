function info (text) {
    console.log(`INFO: ${text}`)
    // trabalhando com o EcmaScript6, observe as crases, é o template do ES6
} 

module.exports = { info } 
//ele sabe que é o nome do atributo e também a referência do método. Aqui vc deixa este método ser chamado do arquivo index.js
